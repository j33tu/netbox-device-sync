name: NetBox Device Type Sync

on:
  # 1. Runs automatically on scheduled cron (Every Monday at midnight)
  schedule:
    - cron: "0 0 * * 1"

  # 2. Runs whenever config/vendors.yml is updated and merged into main
  push:
    branches:
      - main
    paths:
      - "config/vendors.yml"

  # 3. Runs a test check on Pull Requests that modify config/vendors.yml
  pull_request:
    branches:
      - main
    paths:
      - "config/vendors.yml"

  # 4. Allows manual execution anytime from GitHub UI
  workflow_dispatch:

jobs:
  sync:
    name: Sync Device Types
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run NetBox Sync Script
        env:
          NETBOX_URL: ${{ secrets.NETBOX_URL }}
          NETBOX_TOKEN: ${{ secrets.NETBOX_TOKEN }}
        run: python scripts/sync.py
