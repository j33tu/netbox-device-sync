vrfs:
  - name: "Global-ISP-EGRESS"
    rd: "65000:100" # Optional Route Distinguisher (e.g. ASN:NN)
    description: "Global ISP Egress routing context"
    enforce_unique: true # Enforce unique IP space within this VRF

  - name: "Internal-Corporate"
    rd: "65000:200"
    description: "Internal enterprise routing domain"
    enforce_unique: true
